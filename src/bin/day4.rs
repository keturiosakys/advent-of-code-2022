
fn main() {
    let file_data = std::fs::read_to_string("./inputs/day4.txt").expect("Failed to read the file!");
    let parsed: Vec<&str> = file_data.split("\n").collect();
    //let assignments = 


}
